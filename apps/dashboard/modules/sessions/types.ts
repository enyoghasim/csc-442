// Mirrors @attendance/shared's ClassSessionDTO — re-exported under a shorter local alias, same
// reasoning as modules/auth/types.ts and modules/classes/types.ts.
export type { ClassSessionDTO as ClassSession } from '@attendance/shared';
